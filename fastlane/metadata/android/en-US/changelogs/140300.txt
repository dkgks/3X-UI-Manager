New: choose the update channel in Settings — Stable (from GitHub, the default) or Testing (from the home server). Fixed: changes in the outbound, routing rule, balancer and DNS server editors can be saved again — these editors now have Save, which writes to the panel at once.
