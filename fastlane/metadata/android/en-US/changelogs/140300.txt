New: an update channel in Settings — Stable (the tested public releases, the default) or Experimental (pre-release builds that can be unstable). Fixed: changes in the outbound, routing rule, balancer and DNS server editors can be saved again — these editors now have Save, which writes to the panel at once.
